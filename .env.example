# Astra DB Credentials - Required
# Get these from your Astra DB database dashboard

ASTRA_DB_API_ENDPOINT='YOUR_ASTRA_DB_API_ENDPOINT'
ASTRA_DB_TOKEN='YOUR_ASTRA_DB_APPLICATION_TOKEN'
ASTRA_DB_INTEGRATION_OPENAI_KEY_NAME='api_key_name_from_integrations_page'
OPENAI_API_KEY='YOUR_OPENAI_API_KEY'

# Langflow RAG Flow specific endpoint
LANGFLOW_RAG_API_ENDPOINT="YOUR_LANGFLOW_API_ENDPOINT_INCLUDING_FLOW_ID_AND_QUERY_PARAMS"

# Langflow Chatbot end point - leave commented out for now!
#LANGFLOW_PRODUCT_ASSISTANT_ENDPOINT=

# --- Langflow Configuration ---
# Directory for logs, database, etc. Needs to be writable by the user running langflow.
LANGFLOW_CONFIG_DIR="/workspaces/KineticConstructs/.langflow_config"

# Specifies the database file location within the config dir
# Note the four slashes for an absolute path with sqlite:///
LANGFLOW_DATABASE_URL="sqlite:////workspaces/KineticConstructs/.langflow_config/langflow.db"

# Optional: Explicitly set log file path if needed (often inferred from CONFIG_DIR)
# LANGFLOW_LOG_FILE="/workspaces/KineticConstructs/.langflow_config/logs/langflow.log" 

# Optional: Save DB in config dir explicitly (might be default behavior when CONFIG_DIR is set)
# LANGFLOW_SAVE_DB_IN_CONFIG_DIR=true # Optional: Specify collection names if different from defaults
# ASTRA_DB_COLLECTION='products' # Default used if commented out
# ASTRA_DB_PRODUCT_COLLECTION='products' # Default used if commented out
# ASTRA_DB_DOCUMENT_COLLECTION='documents' # Default used if commented out 